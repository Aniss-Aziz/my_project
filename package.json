{
    "name": "my_project",
    "version": "1.0.0",
    "engines": {
        "node": ">=20 <21"
    },
    "scripts": {
        "start": "cd backend && node server.js",
        "start-prod": "cd frontend && ng serve",
        "build": "cd frontend && npm install && npm run build"
    }
}